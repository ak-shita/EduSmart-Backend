services:
  - type: web
    name: edusmart-backend
    env: java
    plan: free
    buildCommand: "javac -cp lib/* src/*.java -d bin"
    startCommand: "java -cp bin;lib/* EduSmartServer"
    envVars:
      - key: PORT
        value: 8080